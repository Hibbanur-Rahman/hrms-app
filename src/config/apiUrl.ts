import API_URL from "../../environmentVariables"

const apiUrls = {
    base_url: API_URL,
    login: "login",
    register: "register",
    forgotPassword:'reset-password',
    getUserDetail:'me',
}

export default apiUrls